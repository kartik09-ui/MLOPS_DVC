1. Creata a git repo and clone it in local
2. Create code.py and add code to it. (It will save as csv file to a new "data" folder)
3. Dio a git add-commit-push before Initialing dvc.
4. Now we do "dvc init" (creates .dvcignore, .dvc)
5. Now we do "mkdir S3" (creates a new S3 directory)
6. Now we do 'dvc remote add -d myremote S3'
7. Next "dvc add data/"
    now it will ask to do: ("git rm -r --cached 'data'" and "git commit -m "stop trancking data"")
    Becuase initially we are tracking data/ folder from git so now we remove it for DVC to handle
8. Again we do "dvc add data/" (creates data.dvc) Then "git add .gitignore data.dvc"
9. Now "dvc commit" and themn "dvc push"
10. Do a git add-commmit-push to mark this stage as first version of data.
11. Now make changes to my_code.py to append a new row in data, check changes via "dvc status".
12. Again "dvc commit" and then "dvc push".